import React from "react";
import { Container, Typography } from "@mui/material";

function LandingPage() {
  return (
    <Container>
      <Typography variant="h2">Welcome to Party Planner!</Typography>
      <Typography variant="body1">
        Join parties, plan activities, and connect with friends.
      </Typography>
    </Container>
  );
}

export default LandingPage;
